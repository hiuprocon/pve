You should put ecj-3.6.2.jar(Eclipse compiler) in this folder.

And you might want to put libraries for ProconVE in this folder.
Required libraries are namely Acerola3D libraries. These include
JAR files and native libraries(Window -> .dll, MacOS X -> .jnilib,
Linux -> .so)

Otherwise, you should configure some settings for required libraries.
